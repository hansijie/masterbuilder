#!groovy 

node {
   stage 'Checkout'
        checkout scm

   stage 'Setup env'
        sh 'echo setup'

   stage 'Build Deploy Package'
        sh 'echo build pkg'

   stage 'Upload Package'
        sh 'echo upload'

   stage 'Deploy to UAT'
        sh 'echo deploy to UAT'
}
